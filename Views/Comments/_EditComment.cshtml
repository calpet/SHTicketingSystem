@model SelfHelpTicketingSystem.Models.CommentViewModel

<head>
    <link href="~/css/comment.css" rel="stylesheet" />
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
</head>

<div class="row" id="full-edit-comment">
    <div>
        <form asp-action="UpdateComment" class="align-content-center" id="edit-comment-form">
            <input hidden asp-for="TicketId" value="@Model.TicketId" />
            <div class="form-group">
                <input hidden asp-for="Text" id="comment-content" value="@Model.Text"/>
                <input hidden asp-for="Text" class="form-control" id="content" rows="12"/>
                <div id="editor" name="editor">
                </div>
                <br/>
                <span asp-validation-for="Text" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" asp-action="UpdateComment" asp-controller="Comments" value="Update" class="btn btn-primary" id="submit-edit" />
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
<script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
<script src="~/js/comment.js"></script>

